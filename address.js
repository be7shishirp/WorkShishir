// Map links: open target google maps with coordinates if you want; placeholders open google maps search
document.querySelectorAll('.place-card .map-link').forEach(a=>{
  // if the href is generic, replace with a better search using the place title
  a.addEventListener('click', (e)=>{
    // allow default; could enhance with coordinates mapping later
  })
});
